## Tests for exported Sophia smart contract verifier

### Requirements

You need to have docker installed.

### Running the tests

Run the tests with `npm run all`, after installing all dependences with `npm install`.